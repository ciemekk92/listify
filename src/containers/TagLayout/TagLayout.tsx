import React from 'react';
import { Wrapper } from './TagLayout.styled';

const TagLayout: React.FC = (props) => {
    return <Wrapper>Placeholder</Wrapper>;
};

export default React.memo(TagLayout);
